name: Update ZKeen → Per-category MSR

on:
  schedule:
    - cron: "0 0 */1 * *"
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.21"
          cache: false

      - name: Prepare folders
        run: |
          mkdir -p scripts lists lists/geosite lists/geoip
          chmod +x scripts/convert.sh || true

      - name: Run converter
        env:
          HOME: /home/runner
        run: |
          ./scripts/convert.sh

      - name: Commit per-category lists
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lists
          if ! git diff --staged --quiet; then
            git commit -m "Auto-update per-category MSR lists"
            git push
          else
            echo "No changes"
          fi
